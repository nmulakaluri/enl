'use strict';

/*describe('Service: orgService', function () {

  // load the service's module
  beforeEach(module('enlApp'));

  // instantiate service
  var orgService;
  beforeEach(inject(function (_orgService_) {
    orgService = _orgService_;
  }));

  it('should do something', function () {
    expect(!!orgService).toBe(true);
  });

});*/
